import { StyleSheet } from 'react-native';

export const createThemedStyles = (colors) =>
  StyleSheet.create({
    // Можно вынести общие стили, но мы будем создавать стили внутри компонентов с помощью useTheme.
  });